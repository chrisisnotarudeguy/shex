=== Shex: an hex editor for SNES ROMs ===

Originally made by p4plus. I forked it just to study and modify it.

=== Installation ===

Somehow install QT libraries, then type 'qmake' in the project directory.
(basically how any QT projects are compiled). Then type 'make debug' or 
'make release'.
